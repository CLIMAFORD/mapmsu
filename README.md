MSU Facility Map

Static web map exported from QGIS (qgis2web). This repository contains a static site served from `index.html`. To host with GitHub Pages, set the Pages source to the `main` branch and root `/`.

Instructions to deploy locally:

1. Serve locally for testing:

```bash
python -m http.server 8000
# open http://localhost:8000
```
